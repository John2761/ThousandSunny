@model IEnumerable<Reserva.Application.DTOs.ReservacionDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>Historial de reservaciones </h1>

<table class="table">
    <thead>
        <tr>
            <th>
                # de reservacion
            </th>
            <th>
                Fecha de reservacio
            </th>
            <th>
                Fecha Limite de Pago
            </th>
            <th>
                Fecha de salida
            </th>
            <th>
                Crucero reservado
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idReservacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaReserva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaLimitePago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idFecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idCrucero)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
